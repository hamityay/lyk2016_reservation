<h2> Add a new Place...</h2>

<% if @place.errors.any?  %>
	<% @place.errors.full_messages.each do |e| %>
		<b><i><%= e %></i></b><br><br>
	<% end %>
<% end %>



<%= render 'form' %>


<br><i><%= link_to 'List places', places_path %></i>